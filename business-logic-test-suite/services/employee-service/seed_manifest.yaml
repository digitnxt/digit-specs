# No session-scoped prerequisites required for this service.
# The Employee Service's cross-module validations (Individual, Keycloak, Boundary)
# are controlled by feature flags. When all flags are disabled the service
# operates standalone with no prerequisites. When flags are enabled, dependency
# data must be seeded in the respective services before employee tests run.
